# Alacritty configuration file

# Window configuration options
window:
  # Window opacity as a floating point number from `0.0` to `1.0`
  # `1.0` is completely opaque and `0.0` is completely transparent.
  opacity: 0.90
